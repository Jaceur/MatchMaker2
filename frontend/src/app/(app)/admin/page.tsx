import { ComingSoon } from "@/components/ComingSoon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="⚙️ Admin"
      note="Qualification bar, allocation, pipeline jobs, and cleanup controls."
    />
  );
}
